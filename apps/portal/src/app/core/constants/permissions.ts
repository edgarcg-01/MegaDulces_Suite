/**
 * `[ID.28]` — Re-export. La definición vive en `@megadulces/contracts`.
 *
 * ⚠️ Esta copia tenía **54 claves de 175: 121 de deriva**, la peor de las
 * cinco. Verificado antes de reemplazarla: las 54 existen en el enum canónico.
 */
export { Permission } from '@megadulces/contracts/authz/permissions';
